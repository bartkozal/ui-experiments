import React from "react";
import ReactDOM from "react-dom";
import FloatingCard from "./FloatingCard";

const rootElement = document.getElementById("root");
ReactDOM.render(<FloatingCard />, rootElement);
